/**
 * @param extraPhoneNum 추가 전화번호
 * @param codeAuthToken 인증토큰
 * @interface
 */

export default interface PostCenterContactsExtraRequestInterface {
    extraPhoneNum: string;
    codeAuthToken: string;
}
